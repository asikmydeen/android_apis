# Copy these into a .env file (or set them in your MCP client config).
# They are the SAME two values the app's Remote tab produces via
# the "Copy connection config" button.

BRIDGE_URL=http://127.0.0.1:8765
BRIDGE_TOKEN=your-token-here
